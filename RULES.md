# Edict 開發規則

## 1. 不留死碼
- 功能改版後，舊流程函數必須刪除，不保留轉發層
- 禁止保留「目前沒有呼叫」的函數、變數、選單項目
- 每次改動後檢查並刪除孤立 import、未使用變數

## 2. 安全規則
- 密碼/密鑰保存在 `.env`，不 hardcode 在程式碼中
- 明碼只允許出現在初始化腳本或說明文件中
- 預設密碼改為生成動態高強度隨機值（`secrets.token_urlsafe(32)`）
- `.env.example` 中的值改用 `CHANGE_ME` 或 `__GENERATE__` 佔位

## 3. 產物與檔案規則
- 測試產物輸出到 `_output/` 目錄
- 不產生用途不明的中間檔、備份檔或舊版腳本
- 每次改動後檢查是否新增無用檔案，若有立刻刪除

## 4. 文件與註解規則
- README 必須同步更新流程、參數與輸出規格
- 程式註解使用中文，且只寫必要內容
- 任何參數新增/刪除都要更新對應章節與範例

## 5. 提交前自檢（必要）
1. 先跑測試做稽核
2. 掃描死碼：確認無未使用函數/變數/選項
3. 互動實跑：curl 測試 API、瀏覽器驗證 UI
4. README 與規則文件已同步
